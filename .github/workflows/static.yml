# DEPRECATED - Use deploy-gh-pages.yml instead
# This workflow is disabled
name: "[DISABLED] Deploy Angular App to Pages"

on:
  # Disabled - no triggers
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow di      git add .
      git commit -m "feat: New deployment strategy using gh-pages branch"
      git push origin mastersabled
        run: echo "This workflow has been replaced by deploy-gh-pages.yml"
